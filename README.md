# Development process guide

Single-page, no-build-step guide to how code ships across ElevateTech and OPS App
(Dev → Staging → Production). Served by GitHub Pages from `main`.

Edit `index.html` directly — keep it diagram-first and short. If this page ever
disagrees with what CI actually does, CI wins; then fix this page.
